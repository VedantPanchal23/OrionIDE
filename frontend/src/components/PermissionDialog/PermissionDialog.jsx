/**
 * Orion IDE — Permission Dialog Component
 * 
 * Modal dialog for approval/rejection at each agent pipeline step.
 * Permission-first: no agent acts without explicit user approval.
 * Will be implemented in a future task.
 */

import React from 'react';

const PermissionDialog = () => {
  return <div id="permission-dialog">{/* Approve/Reject dialog will render here */}</div>;
};

export default PermissionDialog;
