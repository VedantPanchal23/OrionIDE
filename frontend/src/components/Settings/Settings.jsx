/**
 * Orion IDE — Settings Component
 * 
 * Settings panel: theme, font size, language, keybindings.
 * Will be implemented in a future task.
 */

import React from 'react';

const Settings = () => {
  return <div id="settings">{/* Settings form will render here */}</div>;
};

export default Settings;
