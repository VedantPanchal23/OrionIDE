/**
 * Orion IDE — Language Selector Component
 * 
 * Dropdown for selecting programming language for the current file.
 * Will be implemented in a future task.
 */

import React from 'react';

const LanguageSelector = () => {
  return <select id="language-selector">{/* Language options */}</select>;
};

export default LanguageSelector;
