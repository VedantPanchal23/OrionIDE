/**
 * Orion IDE — Topbar Component
 * 
 * Top navigation bar with logo, project name, user menu, settings.
 * Will be implemented in a future task.
 */

import React from 'react';

const Topbar = () => {
  return <div id="topbar">{/* Topbar will render here */}</div>;
};

export default Topbar;
