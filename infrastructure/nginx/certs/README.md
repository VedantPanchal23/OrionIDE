# Place TLS certs here for production nginx.
# Generate local self-signed:
#   bash infrastructure/nginx/generate-certs.sh
# Or on Windows (PowerShell from repo root):
#   .\infrastructure\nginx\generate-certs.ps1
